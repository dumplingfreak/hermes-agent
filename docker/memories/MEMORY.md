Gia is a Verified Investing subscriber (Mega Cap Maestro with Gareth Soloway). When accessing gated/paywalled sites where she has saved credentials, use `open -a 'Google Chrome' <url>` instead of headless browser_navigate — her existing Chrome sessions and saved passwords handle authentication. CDP environment: port 9222 is TradingView Electron, NOT Chrome. Chrome CDP (`--remote-debugging-port=9223`) often fails to bind on her macOS. Reliable fallback: AppleScript JS via `osascript -e 'tell application "Google Chrome" execute active tab of front window javascript "..."'` — requires Chrome's View → Developer → Allow JavaScript from Apple Events. For dynamic content behind expandable toggles, use JS `click()` before extracting `innerText`. Verified Investing uses email code auth (Shop/Shopify) — if not logged in, fill email, get code, submit. After login, profile creation may appear (fill name + UTC+8 timezone).
§
computer_use / cua-driver is NOT available on Gia's Mac and repeatedly fails. User explicitly said "dont use computer use." Fallback: `open -a 'Google Chrome' 'URL'` for web pages, `open -a Safari 'URL'` for Safari. For authenticated sites, `open -a` is the FIRST choice since headless browser_navigate doesn't share Chrome cookies. Do NOT attempt Chrome cookie extraction (user blocked it). Do NOT attempt AppleScript JS against Chrome unless user has explicitly enabled View → Developer → Allow JavaScript from Apple Events.
§
Video ingest task: use deepseek/deepseek-v4-flash exclusively for the entire pipeline (reasoning + vision analysis). Do not use Codex 5.5 or Claude Haiku. This is the standard model for all video ingest work.
§
Gia pays close attention to model selection for specific task types and corrects assumptions. She expects you to check actual config rather than guess, and will override standing skill rules when she wants different behavior. Always verify which model is configured for a task before claiming what you used.
